# 优化策略设计：真实行情样本外验证

## 结论

本报告在上一轮高换手策略失效的基础上，重新设计了 4 个低换手、带 BTC/广度风险门控的原型。
有效性分层：
1. 统计通过：默认与优化均正，且 Bootstrap CAGR 95% CI 下界 > 0；
2. 稳健候选：默认与优化样本外均正；
3. 默认参数有效：默认参数样本外为正，但训练优选参数过拟合失效；
4. 其余仅作研究线索，不建议作为有效策略。

- 训练区间：`2021-01-01` 至 `2024-04-27`
- 样本外区间：`2024-04-28` 至 `2026-07-15`
- BTC 样本外：CAGR `0.9%`，Sharpe `0.25`，最大回撤 `53.0%`
- 成本假设：单边手续费 `0.10%` + 单边滑点 `0.05%`
- 判定汇总：未通过 `1`, 稳健候选（默认与优化均正） `1`, 默认参数有效（优化过拟合） `2`

## 推荐继续关注

- **btc_dual_momentum**（默认参数有效（优化过拟合））：推荐参数 `{'regime_window': 120, 'lookback': 90, 'fast_lookback': 45, 'top_k': 3, 'rebalance_days': 21}`；默认 CAGR `3.7%`，优化 CAGR `-26.1%`；关注 Sharpe `0.32`、最大回撤 `49.3%`。
- **core_satellite_vol_scaled**（默认参数有效（优化过拟合））：推荐参数 `{'regime_window': 120, 'lookback': 60, 'satellite_count': 2, 'rebalance_days': 30, 'core_weight': 0.7, 'vol_scale': 0.7, 'vol_window': 30}`；默认 CAGR `1.1%`，优化 CAGR `-1.7%`；关注 Sharpe `0.22`、最大回撤 `47.9%`。
- **majors_alts_regime**（稳健候选（默认与优化均正））：推荐参数 `{'style_window': 40, 'trend_window': 100, 'top_k': 3, 'rebalance_days': 30}`；默认 CAGR `30.2%`，优化 CAGR `30.2%`；关注 Sharpe `0.89`、最大回撤 `45.4%`。

| 策略 | 思想来源 | 默认 CAGR | 优化 CAGR | Sharpe | 最大回撤 | 样本外换手 | Bootstrap CAGR 95% CI | 判定 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| btc_dual_momentum | 01-趋势轮动 / 02-风险开关 | 3.7% | -26.1% | 0.32 | 49.3% | 25.9 | -43.0%–100.3% | 默认参数有效（优化过拟合） |
| breadth_regime_rotation | 02-全天候 / 05/07-大盘择时 | -2.0% | -1.0% | 0.22 | 46.8% | 20.8 | -45.6%–93.9% | 未通过 |
| core_satellite_vol_scaled | 02-风格轮动 / 03/04-动态仓位 | 1.1% | -1.7% | 0.22 | 47.9% | 11.6 | -39.9%–69.4% | 默认参数有效（优化过拟合） |
| majors_alts_regime | 02-全天候强化 | 30.2% | 30.2% | 0.89 | 45.4% | 9.7 | -26.6%–163.3% | 稳健候选（默认与优化均正） |

## 设计原则

1. **先门控再选股**：BTC 趋势或市场广度不达标时直接持有现金。
2. **降低换手**：默认 14–30 日再平衡，并在选参目标中惩罚年化换手。
3. **双动量过滤**：既要求自身收益为正，再做横截面排序，减少接飞刀。
4. **风险预算**：核心-卫星策略用逆波动率与总敞口缩放控制回撤。
5. **不夸大归因**：这些是受原聚宽思想启发的新原型，不是原策略复现。

## 策略说明

| 策略 | 机制 | 为何可能比上一轮更稳 |
|---|---|---|
| `btc_dual_momentum` | BTC 均线门控 + 长短期双动量 + 自身均线 | 去掉日频追涨，增加确认过滤 |
| `breadth_regime_rotation` | 广度弱/中/强三档仓位 | 用市场宽度替代财务择时 |
| `core_satellite_vol_scaled` | BTC/ETH 核心 + 山寨卫星 + 波动缩放 | 低换手，主动降杠杆 |
| `majors_alts_regime` | 主流/山寨相对强弱 + BTC 趋势过滤 | 强化上一轮唯一弱正信号 |

## 数据证据

- 来源：`https://www.okx.com/api/v5/market/history-candles`
- 周期：`1Dutc`；币对数：`12`
- 行数范围：`2022`–`2022`

## 限制

- 固定使用当前仍在 OKX 交易且历史较长的币对，存在幸存者偏差
- 默认参数虽按经济逻辑设定，但仍经过探索性回测观察，存在研究者自由度
- 日线模型未模拟盘口冲击、稳定币脱锚、交易所故障和下架清算
- 即便样本外点估计为正，宽置信区间仍意味着结果不稳定，不能直接等价于实盘可上线。

详细参数见 `optimized_backtest_results.json`。
